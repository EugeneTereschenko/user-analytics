spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/parcel
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create
      lob.non_contextual_creation: true
      batch_size: 50

  application:
    name=user-analytics-java: